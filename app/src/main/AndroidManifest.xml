<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.appbelajar" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/mulaiplay"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="landscape"
        android:supportsRtl="true"
        android:theme="@style/Theme.Appbelajar" >
        <activity
            android:name=".MainActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".BelajarberhitungActivity"
            android:theme="@style/AppThemeFullScreen" />

        <activity
            android:name=".BelajarActivity"
            android:theme="@style/AppThemeFullScreen" />
        <activity
            android:name=".PenjumlahanActivity"
            android:theme="@style/AppThemeFullScreen" />
        <activity
            android:name=".PenguranganActivity" />
            android:theme="@style/AppThemeFullScreen" />

        <activity
            android:name=".PengucapanActivity" />
            android:theme="@style/AppThemeFullScreen" />
        <activity
            android:name=".PengucapanHurufActivity" />
            android:theme="@style/AppThemeFullScreen" />

    </application>

</manifest>